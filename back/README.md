# Ritto Backend API

## 📋 Описание

Бэкенд-сервис для платформы доставки Ritto, построенный на Node.js и Express. Предоставляет RESTful API для управления продуктами, заказами, пользователями, корзинами и платежами.

## 🛠 Технологический стек

- **Runtime**: Node.js
- **Framework**: Express.js
- **Database**: Supabase (PostgreSQL)
- **ORM**: Sequelize
- **Authentication**: JWT (jsonwebtoken)
- **Payment**: Stripe
- **Security**: Helmet, XSS-Clean, HPP
- **Validation**: Express-validator, Zod
- **Testing**: Jest, Supertest

## 📁 Структура проекта

```
back/
├── __tests__/          # Тесты
├── config/             # Конфигурационные файлы
├── controllers/        # Контроллеры бизнес-логики
│   ├── adminController.js
│   ├── authController.js
│   ├── cartController.js
│   ├── discountController.js
│   ├── orderController.js
│   ├── orderItemController.js
│   ├── paymentController.js
│   └── productController.js
├── middleware/         # Промежуточные обработчики
│   ├── authMiddleware.js
│   └── errorMiddleware.js
├── routes/            # Маршруты API
│   ├── adminRoutes.js
│   ├── authRoutes.js
│   ├── cartRoutes.js
│   ├── discountRoutes.js
│   ├── orderRoutes.js
│   ├── orderItemRoutes.js
│   ├── paymentRoutes.js
│   └── productRoutes.js
├── supabase/          # Интеграция с Supabase
├── utils/             # Вспомогательные функции
├── validators/        # Валидаторы данных
├── .env.example       # Пример переменных окружения
├── server.js          # Точка входа приложения
└── package.json       # Зависимости проекта
```

## 🚀 Установка и запуск

### Требования

- Node.js >= 14.x
- npm или yarn
- PostgreSQL (через Supabase)

### Установка

```bash
# Клонировать репозиторий
git clone <repository-url>
cd back

# Установить зависимости
npm install

# Настроить переменные окружения
cp .env.example .env
# Отредактировать .env файл с вашими настройками
```

### Переменные окружения

Создайте файл `.env` на основе `.env.example`:

```env
PORT=5000
NODE_ENV=development
JWT_SECRET=your_jwt_secret_key
SUPABASE_URL=your_supabase_url
SUPABASE_ANON_KEY=your_supabase_anon_key
STRIPE_SECRET_KEY=your_stripe_secret_key
ADMIN_EMAIL=admin@example.com
CLIENT_URL=http://localhost:5173
```

### Запуск

```bash
# Development режим с hot-reload
npm run dev

# Production режим
npm start

# Запуск тестов
npm test
```

Сервер запустится на `http://localhost:5000`

## 📡 API Endpoints

### Аутентификация (`/api/auth`)

- `POST /api/auth/register` - Регистрация нового пользователя
- `POST /api/auth/login` - Вход пользователя
- `POST /api/auth/logout` - Выход пользователя
- `GET /api/auth/me` - Получить данные текущего пользователя

### Продукты (`/api/products`)

- `GET /api/products` - Получить список всех продуктов
- `GET /api/products/:id` - Получить продукт по ID
- `POST /api/products` - Создать новый продукт (admin)
- `PUT /api/products/:id` - Обновить продукт (admin)
- `DELETE /api/products/:id` - Удалить продукт (admin)

### Заказы (`/api/orders`)

- `GET /api/orders` - Получить заказы пользователя
- `GET /api/orders/:id` - Получить заказ по ID
- `POST /api/orders` - Создать новый заказ
- `PUT /api/orders/:id` - Обновить статус заказа
- `DELETE /api/orders/:id` - Отменить заказ

### Корзина (`/api/cart`)

- `GET /api/cart` - Получить корзину пользователя
- `POST /api/cart` - Добавить товар в корзину
- `PUT /api/cart/:id` - Обновить количество товара
- `DELETE /api/cart/:id` - Удалить товар из корзины
- `DELETE /api/cart` - Очистить корзину

### Платежи (`/api/payment`)

- `POST /api/payment/create-intent` - Создать платёжное намерение (Stripe)
- `POST /api/payment/confirm` - Подтвердить платёж
- `GET /api/payment/:id` - Получить статус платежа

### Скидки (`/api/discounts`)

- `GET /api/discounts` - Получить список скидок
- `POST /api/discounts` - Создать скидку (admin)
- `PUT /api/discounts/:id` - Обновить скидку (admin)
- `DELETE /api/discounts/:id` - Удалить скидку (admin)

### Админ панель (`/api/admin`)

- `GET /api/admin/orders` - Получить все заказы
- `GET /api/admin/users` - Получить всех пользователей
- `GET /api/admin/stats` - Получить статистику
- `PUT /api/admin/orders/:id/status` - Обновить статус заказа

### Элементы заказа (`/api/order_items`)

- `GET /api/order_items` - Получить элементы заказа
- `POST /api/order_items` - Создать элемент заказа
- `PUT /api/order_items/:id` - Обновить элемент заказа
- `DELETE /api/order_items/:id` - Удалить элемент заказа

## 🔒 Безопасность

Проект использует следующие меры безопасности:

- **Helmet**: Защита HTTP заголовков
- **XSS-Clean**: Защита от XSS атак
- **HPP**: Защита от HTTP Parameter Pollution
- **JWT**: Токены для аутентификации
- **CORS**: Настроенный CORS для безопасных запросов
- **Rate Limiting**: Ограничение количества запросов
- **Validation**: Валидация данных через express-validator и Zod

## 🧪 Тестирование

```bash
# Запустить все тесты
npm test

# Запустить тесты в watch режиме
npm test -- --watch

# Запустить тесты с coverage
npm test -- --coverage
```

## 🗄️ База данных

Проект использует Supabase (PostgreSQL) в качестве основной базы данных.

### Основные таблицы:

- `users` - Пользователи
- `products` - Продукты
- `orders` - Заказы
- `order_items` - Элементы заказа
- `cart` - Корзины покупок
- `discounts` - Скидки и промокоды
- `payments` - Информация о платежах

## 🔄 Middleware

### authMiddleware.js
Проверка JWT токенов и авторизация пользователей

### errorMiddleware.js
Централизованная обработка ошибок

## 📝 Логирование

Используется Morgan для логирования HTTP запросов в development режиме.

## 🐳 Docker

Проект можно запустить через Docker:

```bash
# Сборка образа
docker build -t ritto-backend .

# Запуск контейнера
docker run -p 5000:5000 --env-file .env ritto-backend
```

## 📦 Основные зависимости

- `express` - Web фреймворк
- `@supabase/supabase-js` - Клиент Supabase
- `sequelize` - ORM для работы с БД
- `jsonwebtoken` - JWT аутентификация
- `stripe` - Платёжная система
- `bcrypt/bcryptjs` - Хеширование паролей
- `cors` - CORS middleware
- `helmet` - Security middleware
- `express-validator` - Валидация данных
- `zod` - Schema validation

## 🤝 Интеграции

### Supabase
Используется для:
- Хранение данных (PostgreSQL)
- Аутентификация
- Real-time subscriptions (опционально)

### Stripe
Используется для:
- Обработка платежей
- Создание платёжных намерений
- Webhook обработка

## 🔧 Конфигурация

Конфигурационные файлы находятся в папке `config/`:
- База данных
- JWT настройки
- Stripe настройки
- CORS настройки

## 📊 Мониторинг

В production режиме:
- Логи ошибок не показывают stack trace
- Используется сокращённый формат логирования
- Скрываются чувствительные данные

## 🚨 Обработка ошибок

Все ошибки централизованно обрабатываются через error middleware:
- 400 - Bad Request
- 401 - Unauthorized
- 403 - Forbidden
- 404 - Not Found
- 500 - Internal Server Error

## 💡 Best Practices

1. Всегда используйте валидацию входных данных
2. Не храните секреты в коде - используйте .env
3. Используйте async/await для асинхронных операций
4. Обрабатывайте все ошибки должным образом
5. Используйте JWT для защищённых маршрутов
6. Применяйте rate limiting для предотвращения злоупотреблений

## 📄 Лицензия

ISC

## 👥 Контакты

Для вопросов и предложений обращайтесь к команде разработки.
