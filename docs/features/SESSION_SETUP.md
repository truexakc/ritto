# Настройка сессий для корзины

## 📦 Установка зависимостей

Зайдите в контейнер бэкенда и установите новые пакеты:

```bash
# Войти в контейнер
docker exec -it ritto-backend sh

# Установить зависимости
npm install express-session connect-pg-simple

# Выйти из контейнера
exit
```

## 🗄️ Создание таблицы сессий

Таблица для сессий уже создана в миграции `back/migrations/002_session_table.sql`.

Если миграция не применилась автоматически, выполните вручную:

```bash
docker exec -i ritto-postgres psql -U ritto_user -d ritto_db < back/migrations/002_session_table.sql
```

Или через psql:

```bash
docker exec -it ritto-postgres psql -U ritto_user -d ritto_db

-- Выполните SQL из файла 002_session_table.sql
CREATE TABLE IF NOT EXISTS session (
    sid VARCHAR NOT NULL COLLATE "default",
    sess JSON NOT NULL,
    expire TIMESTAMP(6) NOT NULL,
    PRIMARY KEY (sid)
);

CREATE INDEX IF NOT EXISTS IDX_session_expire ON session (expire);
```

## 🔐 Настройка переменных окружения

Добавьте в ваш `.env` файл:

```env
SESSION_SECRET=your_session_secret_key_here_change_in_production
```

**Важно:** Используйте надежный случайный ключ в продакшене!

Сгенерировать случайный ключ можно так:

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

## 🔄 Перезапуск контейнеров

После установки зависимостей и настройки переменных окружения:

```bash
# Пересобрать и перезапустить бэкенд
docker-compose up -d --build backend

# Или перезапустить все сервисы
docker-compose restart
```

## ✅ Проверка работы

### 1. Проверьте, что таблица создана:

```bash
docker exec ritto-postgres psql -U ritto_user -d ritto_db -c "\d session"
```

Должна показаться структура таблицы.

### 2. Проверьте логи бэкенда:

```bash
docker logs ritto-backend --tail 20
```

Должно быть сообщение: `✅ Успешное подключение к PostgreSQL`

### 3. Протестируйте корзину:

- Откройте сайт БЕЗ авторизации
- Добавьте товары в корзину
- Проверьте, что сессия создалась:

```bash
docker exec ritto-postgres psql -U ritto_user -d ritto_db -c "SELECT sid, expire FROM session LIMIT 5;"
```

## 🐛 Возможные проблемы

### Ошибка: "Cannot find module 'express-session'"

**Решение:** Установите зависимости в контейнере:
```bash
docker exec -it ritto-backend npm install
docker restart ritto-backend
```

### Ошибка: "relation "session" does not exist"

**Решение:** Создайте таблицу вручную (см. раздел "Создание таблицы сессий")

### Сессии не сохраняются

**Решение:** 
1. Проверьте, что `SESSION_SECRET` установлен в `.env`
2. Проверьте, что таблица `session` существует
3. Проверьте логи: `docker logs ritto-backend`

## 📝 Дополнительная информация

- Сессии хранятся в PostgreSQL (таблица `session`)
- Время жизни сессии: 30 дней
- Cookie с флагом `httpOnly` для безопасности
- Автоматическая очистка истекших сессий

## 🎯 Готово!

После выполнения всех шагов корзина будет работать как для авторизованных, так и для неавторизованных пользователей.
