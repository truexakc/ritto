#!/bin/bash

# Скрипт для обновления зависимостей бэкенда

echo "🔄 Обновление зависимостей бэкенда..."

cd back

# Удаляем старый package-lock.json и node_modules
rm -rf package-lock.json node_modules

# Устанавливаем зависимости заново
npm install

echo "✅ Зависимости обновлены!"
echo "📦 Теперь можно пересобрать Docker контейнер:"
echo "   docker-compose up -d --build backend"
